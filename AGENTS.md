---
apply: 始终
---

# AGENTS.md

本规则面向在 `skin-hub` 仓库内工作的 AI 编码助手。目标是少问多做、先找根因、改动聚焦、验证可复查。

## 1. 沟通与协作

- 默认使用中文回复，除非用户明确要求其他语言。
- 先给结论或当前结果，再补充必要背景；日常回复保持简短、可执行。
- 多步骤编码任务首次调用工具前，先用 1-2 句话说明准备做什么。
- 用户意图清楚且风险低时，补齐合理细节并直接推进；只有缺失信息会明显改变方案、破坏数据或影响安全时才提问。
- 不主动扩展无关功能，不做宽泛重写，不追加用户未要求的“顺手优化”。
- 发现自己判断错误时，直接承认并修正，不做冗长解释。

## 2. 项目事实

- 本仓库是 Git 工作副本；不要按 SVN 流程处理。
- 项目是 `Next.js App Router + TypeScript` 前端应用，核心代码位于 `src/`。
- 后续开发需要优先遵循当前项目已有的技术栈、目录结构和代码风格。
- 常用脚本：`npm run dev`、`npm run build`、`npm run start`、`npm run lint`。
- 路由位于 `src/app`，复用组件位于 `src/components`，接口封装位于 `src/lib/api`，SEO 工具位于 `src/lib/seo`，类型定义位于 `src/types`。
- 使用 `@/*` 指向 `src/*` 的路径别名。
- 远程图片白名单配置在 `next.config.ts`，涉及图片域名时必须同步检查。
- `node_modules/`、`.next/`、构建产物、日志、临时文件和密钥文件不要手工修改或提交。
- 源码和文档统一按 UTF-8 处理；发现乱码时优先修复编码来源，而不是继续复制乱码。

## 3. 项目文档目录约定

- 相关项目文档统一放在 `docs/` 目录下。
- `docs/prd.md`：存放项目需求文档、功能规格、已确认的产品决策。
- `docs/images/`：存放截图、界面参考图、流程图等图片资料。
- `docs/api/`：存放 API 文档、接口约定、第三方服务对接说明。

## 4. 需求沉淀规则

- 当讨论功能实现、产品需求、业务规则或交互细节时，如果某些需求已经明确，需要及时整理并写入 `docs/prd.md`。
- 写入 PRD 前，必须基于已经确认的信息，不要把未经确认的猜测当成最终需求。
- 如果需求还不完整，可以先记录为“待确认问题”或“开放问题”。

## 5. 工作流程

- 先读现有代码、相邻实现、配置和文档，再制定方案；搜索优先用 `rg`/`rg --files`。
- 非平凡任务先形成短计划：目标或根因、影响范围、局部修复还是结构修复、实现路径、验证方式。
- 小步修改，保持 diff 聚焦；每次只处理与当前任务直接相关的文件。
- 发现现有未提交改动时，视为用户改动：不要回滚、不要覆盖；若影响当前任务，先理解并在其基础上修改。
- 改完后复查 `git diff`，确认没有症状补丁、重复逻辑、隐藏兜底、过宽异常吞噬、死代码、未说明的行为变化或安全回退。

## 6. 修复与代码质量

- 修 bug 要追根因，不只消除表面报错；让错误以明确异常、日志、失败测试或可复现现象暴露。
- 禁止为“看起来能跑”而新增静默兜底、模拟成功路径、吞异常的大 `try/catch`、隐藏默认值或第二套校验逻辑。
- 涉及重复业务逻辑、多处事实源、共享校验/权限/缓存、接口契约、路由、状态同步、数据完整性或安全边界时，按结构性问题处理：统一不变量，删掉旧逻辑，而不是叠加旁路。
- 遵循项目现有分层、命名、组件拆分和样式习惯；优先复用已有组件、工具函数、类型和配置。
- TypeScript 保持 `strict` 友好；不要用 `any`、非空断言或类型断言掩盖真实类型问题，除非边界处有明确理由。
- React/Next.js 优先使用服务端组件承载数据获取和 SEO 内容；只有需要浏览器状态、事件或副作用时才使用客户端组件。
- 函数保持短小，控制流尽量扁平；使用早返回和具名常量，避免魔法值。
- 注释只解释意图、约束或取舍，不复述代码。
- 不硬编码密钥、账号、Token、证书密码等敏感信息；外部输入进入 URL、命令或接口参数前必须显式约束。

## 7. 前端与 SEO 要求

- 页面首屏应直接输出可索引的主体内容，不把核心内容完全推迟到客户端渲染。
- 列表页、详情页、聚合页需要维护稳定 URL、metadata、canonical、Open Graph 和必要的结构化数据。
- 图片必须有稳定尺寸策略和有意义的 `alt`；优先复用 `src/lib/images` 与项目现有图片组件策略。
- PC 优先但必须保证移动端核心路径可用；避免只在桌面端验证布局。
- 样式改动遵循现有视觉系统；如果是新页面，避免默认模板感，明确排版、色彩和背景方向。

## 8. 验证

- 有代码改动时，按影响范围优先执行：
  1. 变更行为的定向测试或最小复现。
  2. `npm run lint`。
  3. `npm run build`。
  4. 必要的页面或接口烟测。
- 不把“服务启动了”当成页面验证；需要实际访问关键页面或执行关键路径。
- 验证无法运行时，明确说明原因、已做的替代检查和剩余风险。
- 长耗时验证要说明原因，并优先选择能证明本次改动的最小检查。

## 9. Git、文档与外部资料

- 常用检查命令为 `git status`、`git diff`、`git diff --stat`；不执行 `git commit`，除非用户明确要求。
- 提交信息对齐近期历史，优先使用 `type(scope): 中文说明`，例如 `feat(homepage): 优化移动端适配`、`docs(readme): 更新项目架构说明`。
- 不提交或夹带 `.idea/`、`.next/`、`node_modules/`、构建产物、日志、临时文件、密钥文件或与当前任务无关的改动。
- 行为、接口、部署或使用方式发生变化时，同步更新相关文档；不要为小修小补制造无关文档。
- 引用外部资料时标明来源 URL；涉及当前事实、版本、价格、法规、接口文档等易变内容时必须联网核实。
- 规则冲突时，优先级为：用户最新明确指令 > 当前目录或更近范围的规则 > 本文件 > 其他参考资料。
